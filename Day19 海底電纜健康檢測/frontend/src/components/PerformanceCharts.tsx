
import React from 'react';
export default function PerformanceCharts(){ return (<div style={{minHeight:240,display:'grid',placeItems:'center'}}><div className="muted">M‑Lab（BigQuery）目前未啟用。若需終端體感指標，請執行 <code>scripts\enable-mlab.bat</code> 並設定 <code>backend\.env</code> 後再重啟。</div></div>); }
